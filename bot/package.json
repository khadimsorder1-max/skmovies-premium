{
  "name": "skmovies-bot",
  "version": "1.0.0",
  "description": "SKMovies Telegram Bot — premium movie bot with commands, Mini App, pixel-perfect clone of mlsbd.co",
  "main": "src/index.js",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "setup": "wrangler deploy && echo 'Visit: https://<your-worker>.workers.dev/setup?token=<BOT_TOKEN>&webhook=1'",
    "tail": "wrangler tail"
  },
  "devDependencies": { "wrangler": "^3.78.0" },
  "engines": { "node": ">=18" }
}
